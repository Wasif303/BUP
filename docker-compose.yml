version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      # Add API keys or other environment variables here
      # - GEMINI_API_KEY=your_api_key_here
